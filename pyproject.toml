[tool.poetry]
name = "site-devops"
version = "0.1.0"
description = ""
authors = ["Lucas Lima"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
flask = "^3.0.2"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
